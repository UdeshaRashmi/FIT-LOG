// placeholder for email sending helper
exports.sendEmail = async ({ to, subject, html }) => {
  console.log(`Sending email to ${to} - subject: ${subject}`);
  // integrate nodemailer at application level when ready
};
